<nav class="navbar navbar-expand-lg navbar-light bg-light px-4" style="height: 100px;">
    <a class="navbar-brand" href="{{ path('app_homepage') }}">
        <p class="pl-2 d-inline font-weight-bold brand-name">Animax</p>
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            {% if app.user %}
                <li class="nav-item">
                    <a class="navbar-links" href="{{ path('app_shopping_cart') }}"><i class="fa fa-shopping-cart"></i> Cart</a>
                </li>
                <li class="nav-item">
                    <a class="navbar-links" href="{{ path('app_orders_list') }}">Orders</a>
                </li>
                <li class="nav-item">
                    <a class="navbar-links" href="{{ path('app_logout') }}">Log Out</a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="navbar-links" href="{{ path('app_register') }}">Register</a>
                </li>
                <li class="nav-item">
                    <a class="navbar-links" href="{{ path('app_login') }}">Log In</a>
                </li>
            {% endif %}
        </ul>
    </div>
    <div class="form-inline d-flex">
        <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search" id="productName" value="{{ productName|default }}">
        <button class="btn btn-outline-success mx-2" onclick="updatePageByFilters('{{ path('app_products') }}')">Search</button>
    </div>
</nav>
