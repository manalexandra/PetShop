<nav class="navbar navbar-expand-lg navbar-light bg-light px-4" style="height: 100px;">
    <a class="navbar-brand" href="{{ path('app_homepage') }}">
        <p class="pl-2 d-inline font-weight-bold" style="color: mediumseagreen; font-size: xxx-large">Animax</p>
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_shopping_cart') }}" style="color: #444444"><i class="fa fa-shopping-cart"></i> Cart</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_orders_list') }}" style="color: #444444">Orders</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}" style="color: #444444">Log Out</a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_register') }}" style="color: #444444">Register</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_login') }}" style="color: #444444">Log In</a>
                </li>
            {% endif %}
        </ul>
    </div>
    <form class="form-inline d-flex">
        <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-success mx-2" type="submit">Search</button>
    </form>
</nav>